[General]
* Unit tests?
* Create "examples.ks"
* Run profiler?
* Github Actions for release?
* Namespace all functions and avoid global pollution?

[main.ks]
* Planet intercept refinement

[validate.ks]
* Impatience factor?
* Extract search_interval to settings
* Extract search step size, threshold and factor to settings

// Time to escape SOI
//local n is sqrt(mu / a ^ 3).
//local cos_theta is (a * (e ^ 2 - 1) - r2) / (e * r2).
//local cosh_h is (cos_theta + e) / (1 + e * cos_theta).
//local h is ln(cosh_h + sqrt(cosh_h ^ 2 - 1)).
//local sinh_h is (constant:e ^ h - constant:e ^ (-h)) / 2.
//local t is (e * sinh_h - h) / n.